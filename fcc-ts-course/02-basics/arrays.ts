const super_heroes: string[] = [];
// const powers: number[] = [];
const powers: Array<number> = [];

super_heroes.push("spiderman");
powers.push(2);

type User = {
  name: string;
  id: number;
};

const users: User[] = [];

users.push({ name: "arni", id: 0 });

const models: number[][] = [[255, 255, 255]];

export {};
